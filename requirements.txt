anthropic>=0.40.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.1
openai>=1.50.0
trimesh>=4.0.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0
rich>=13.7.0
click>=8.1.0
numpy-stl>=3.0.0
httpx>=0.27.0

# ── Remote desktop ────────────────────────────────────────────────────────────
mss>=9.0.1           # screen capture (cross-platform, Retina-aware)
pynput>=1.7.7        # mouse + keyboard control
pyotp>=2.9.0         # TOTP 2FA
qrcode>=7.4.2        # QR code generation for authenticator setup
bcrypt>=4.1.0        # password hashing
websockets>=12.0     # WebSocket support (pulled by fastapi, pinned here)

# trimesh optional deps needed for mesh slicing (print-bed splitting)
scipy>=1.11.0
shapely>=2.0.0
mapbox-earcut>=2.0.0

# ── Mesh backends (install whichever you use) ──────────────────────────────
# shape-e (local/free, ~1 GB model download on first use):
#   pip install shap-e torch
#
# tripo3d / meshy (cloud APIs, httpx already covers these):
#   just set the API key in .env — no extra packages needed
